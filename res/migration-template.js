module.exports = {
    up: function(done) {
        // pass in an error if something goes wrong
        done();
    },

    down: function(done) {
        // pass in an error if something goes wrong
        done();
    }
};
